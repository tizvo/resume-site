# Автоматически заполняемый сайт-резюме на Python

## Описание
Сайт-резюме, который получает данные из GitHub API и отображает их на странице.

## Установка и настройка

1. **Создайте проект:**
   - Создайте папку `resume-site` и добавьте файлы.

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt